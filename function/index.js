function getName1(name) {
   const name1 = 'Vasya';
    return name = `Имя равно ${name1}`;
}
console.log(getName1(name));

const getName2 = function(name) {
    const name1 = 'Petya';
    return name = `Имя равно ${name1}`;

} 
console.log(getName2(name));

const getName3 = (name) => {
    const name1 = 'Sasha';
    return name = `Имя равно ${name1}`;
}
console.log(getName3(name));