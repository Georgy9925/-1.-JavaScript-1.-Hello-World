function getSumOfSequence(number) {
   let array = [1,2,3,4,5,6];
    
    for (let i = 0; i <= number ; i++) {

            return  array[0] + array.pop(i) } 
        
  }
        console.log(getSumOfSequence(6))

